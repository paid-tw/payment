node_modules
dist
coverage
*.tsbuildinfo
.DS_Store
.env
.env.*
!.env.example

# Captured ECPay ReturnURL notifies (scripts/ecpay-notify-capture.mjs)
*.jsonl

# `pnpm pack` output — docs/release.md tells you to run it, so it will appear here.
*.tgz
